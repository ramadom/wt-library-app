{% extends 'base.html.twig' %}

{% block body %}

    <div id="book-list">
        <table id="bookTable">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Genre</th>
                    <th>ISBN</th>
                </tr>
            </thead>
            <tbody>
        {% for book in books %}
            <tr>
                <td>{{ book.title }}</td>
                <td>{{ book.genre }}</td>
                <td>{{ book.ISBN }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">No books in library</td>
            </tr>
        {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
